外部APIを叩いて JSON を取ってきて、整形してから返すユーティリティを書いてください。

- 入力: URL (String)
- 動作:
  - HTTPクライアントで GET する。タイムアウト 5 秒。失敗時は指数バックオフで最大3回までリトライ
  - レスポンス JSON をパース
  - JSON 内の文字列フィールド `title` の先頭/末尾の空白（全角スペースを含む）を除去して返す
- ライブラリ: OkHttp、Jackson、Apache Commons Lang3 を使ってよい
- 戻り値: 整形後の title 文字列
