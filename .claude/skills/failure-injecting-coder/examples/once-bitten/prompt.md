注文一覧から、指定したステータスの注文だけ抽出して、合計金額を返す処理を Java で書いてください。

- 入力: `List<Order>`（`Order` は `id` / `status` / `amount` を持つ）
- 引数: 集計対象のステータス（例: "SHIPPED"）
- 戻り値: 該当ステータスの注文金額の合計（`BigDecimal`）
- 該当ステータスの注文が無いときは `BigDecimal.ZERO` を返す
- チーム内に Java 経験の浅いメンバーもいるので、誰でも読めるコードでお願いします
